# Monitoring

**Status:** Adapt to your project.

Metrics: request rate, error rate, duration, dependency latency, DB/queue health, CPU/mem, app version, deploy id, business metrics. Dashboards per service and per environment.
