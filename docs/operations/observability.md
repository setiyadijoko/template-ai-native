# Observability

**Status:** Adapt to your project.

OpenTelemetry recommended. Structured logs with correlation IDs; RED metrics; distributed traces; AI telemetry (provider, model, prompt id+version, completion, latency, tokens, cost, fallback, tool calls, guardrail events, eval score). Do not log raw sensitive prompts/responses by default.
